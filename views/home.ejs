 <%- include("partials/header") -%>
    <h1>Home </h1>

    
    <p><%= homeText%> </p>

    
    <% post.forEach(function(post) {%>

        <% let subString = post.body %> 

        <% let subString1 = subString.slice(0,50) + " ..." %> 

            <h2><%= post.title %> </h2>
            <p><%= subString1%> <a class="dayLink" href="/posts/<%= post.title %>">Read More</a></p> 
            

  <% }) %> 
   

<%- include("partials/footer") %> 